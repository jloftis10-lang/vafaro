"use client";
import Link from "next/link";
import { FormEvent, useMemo, useState } from "react";
import { useSearchParams } from "next/navigation";
import { ArrowRight, CheckCircle, Compass, Copy, Info, SealCheck, Warning } from "@phosphor-icons/react";
import { decodeReport } from "@/lib/assessment";

const icons={high:Warning,check:Info,good:CheckCircle};
export function ResultsClient(){
  const params=useSearchParams();
  const report=useMemo(()=>decodeReport(params.get("r")??""),[params]);
  const [copied,setCopied]=useState(false); const [email,setEmail]=useState(""); const [saved,setSaved]=useState(false);
  if(!report)return <main className="results-empty"><h1>We couldn’t open this report.</h1><Link className="button" href="/check">Start a new check</Link></main>;
  const unknowns=report.unknowns??["Exact walking routes and terrain","Current access details at each venue","Live conditions and provider changes"];
  const confidence=report.confidence??report.findings.reduce((summary,finding)=>{if(finding.confidence==="Based on your profile")summary.profileBased+=1;if(finding.confidence==="AI estimate")summary.estimated+=1;if(finding.confidence==="Official source needed")summary.needsVerification+=1;return summary},{profileBased:0,estimated:0,needsVerification:0});
  const share=async()=>{try{await navigator.clipboard.writeText(window.location.href);setCopied(true)}catch{setCopied(false)}};
  const capture=(event:FormEvent)=>{event.preventDefault();localStorage.setItem("vafaro-email",email);setSaved(true)};
  return <main className="results-page"><header className="nav shell"><Link className="brand" href="/"><span className="brand-mark"><Compass size={20} weight="fill"/></span>vafaro</Link><button className="share-button" onClick={share}><Copy size={16}/>{copied?"Link copied":"Share report"}</button></header><section className="results-hero shell"><div><p className="section-kicker">YOUR FREE FAMILY TRIP SCAN</p><h1>{report.title}</h1><p className="results-summary">{report.summary}</p></div><div className="result-score"><strong>{report.score}</strong><span>PLANNING<br/>SIGNAL</span></div></section><section className="result-layout shell"><aside><p>PROFILE CHECKED</p>{report.input.companions.map(item=><span key={item}>✓ {item}</span>)}{report.input.needs.map(item=><span key={item}>✓ {item}</span>)}</aside><div className="result-main"><section className="result-disclaimer"><Info size={20} weight="fill"/><p><strong>This is a planning signal, not a safety or accessibility certification.</strong> It is based on your answers and general heuristics. Conditions and provider details must be confirmed directly before booking.</p></section><p className="section-kicker">WHAT TO KNOW BEFORE YOU BOOK</p>{report.findings.slice(0,3).map((finding,index)=>{const Icon=icons[finding.level];return <article className={`live-finding ${finding.level}`} key={finding.title}><span className="finding-number">0{index+1}</span><Icon size={24} weight="fill"/><div><p>{finding.level==="high"?"HIGH FRICTION":finding.level==="good"?"STRONG FIT":"CHECK THIS"}</p><h2>{finding.title}</h2><span>{finding.detail}</span><div className="action"><strong>VAFARO SUGGESTS</strong>{finding.action}</div><small>{finding.confidence}</small></div></article>})}<section className="confidence-panel"><div><p className="section-kicker">HOW CERTAIN IS THIS SCAN?</p><h2>Confidence is part of the answer.</h2></div><dl><div><dt>{confidence.profileBased}</dt><dd>Based on your profile</dd></div><div><dt>{confidence.estimated}</dt><dd>Planning estimates</dd></div><div><dt>{confidence.needsVerification}</dt><dd>Need direct verification</dd></div></dl></section><section className="unknowns-panel"><p className="section-kicker">WHAT WE COULD NOT VERIFY</p><h2>These unknowns can change the recommendation.</h2><ul>{unknowns.map(item=><li key={item}>{item}</li>)}</ul></section><section className="email-capture"><SealCheck size={30} weight="fill"/><div><h2>{saved?"Your interest is saved in this browser.":"Join the founding-family list"}</h2><p>{saved?"Email delivery is not connected yet, so no message has been sent.":"Tell us you want a human-reviewed family trip check. This prototype saves your interest only in this browser and does not send email yet."}</p>{saved?null:<form onSubmit={capture}><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" aria-label="Email address"/><button className="button">Save my interest <ArrowRight size={16}/></button></form>}</div></section><section className="upgrade-card"><p>FOUNDING FAMILY REVIEW · PROPOSED $119</p><h2>A real person investigates the unknowns that could change your trip.</h2><ul><li>Complete itinerary review</li><li>Walking, pacing, and transfer analysis</li><li>Priority access details checked</li><li>Practical family-friendly corrections</li></ul><button className="button" onClick={()=>document.querySelector<HTMLInputElement>(".email-capture input")?.focus()}>Join the founding-family list <ArrowRight size={17}/></button><small>Early concept price. Scope and price are confirmed before any payment.</small></section></div></section></main>;
}
